# Database Backup Configuration
# Copy this file to .env.backup and fill in your actual credentials

# Database Configuration
DB_TYPE=mysql                           # mysql or postgresql
DB_HOST=localhost
DB_PORT=3306                            # 3306 for MySQL, 5432 for PostgreSQL
DB_NAMES=your_database_name             # Comma-separated for multiple DBs: db1,db2,db3
DB_USER=your_db_user
DB_PASSWORD=your_db_password

# Cloudflare R2 Configuration
# Get your credentials from: https://dash.cloudflare.com/?to=/:account/r2/overview/api-tokens
S3_ENDPOINT=https://YOUR_ACCOUNT_ID.r2.cloudflarestorage.com
S3_BUCKET=your-backup-bucket
S3_ACCESS_KEY=your_r2_access_key_id
S3_SECRET_KEY=your_r2_secret_access_key
S3_REGION=auto                          # R2 uses 'auto' for region

# Backup Configuration
BACKUP_DIR=/tmp
LOG_FILE=/var/log/database-backup.log
